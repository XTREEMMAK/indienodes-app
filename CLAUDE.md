# Security review

For any change touching auth/the gate, form or webhook handling, user-supplied input,
CSP/CORS, file uploads, external API calls, or credentials — invoke the `security-review`
skill before proposing or writing it.
